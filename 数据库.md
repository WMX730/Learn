# MongoDB 与 SQL 数据库对比学习资料

> 本文用于系统理解 MongoDB 与传统 SQL / 关系型数据库的差异。  
> 这里的 SQL 数据库主要指 MySQL、PostgreSQL、Oracle、SQL Server 等关系型数据库；MongoDB 代表典型文档型 NoSQL 数据库。  
> 重点从使用场景、数据模型、架构、查询方式、事务、一致性、扩展能力、底层逻辑、性能优化和选型建议等方面对比。

---

## 目录

1. [一句话理解 MongoDB 和 SQL 数据库](#1-一句话理解-mongodb-和-sql-数据库)
2. [核心差异总览](#2-核心差异总览)
3. [数据模型对比](#3-数据模型对比)
4. [使用场景对比](#4-使用场景对比)
5. [架构设计对比](#5-架构设计对比)
6. [底层存储逻辑对比](#6-底层存储逻辑对比)
7. [查询语言和开发体验对比](#7-查询语言和开发体验对比)
8. [事务与一致性对比](#8-事务与一致性对比)
9. [扩展能力对比](#9-扩展能力对比)
10. [索引与性能优化对比](#10-索引与性能优化对比)
11. [表关联、嵌套和聚合对比](#11-表关联嵌套和聚合对比)
12. [Schema 设计思路对比](#12-schema-设计思路对比)
13. [典型业务建模对比](#13-典型业务建模对比)
14. [优缺点总结](#14-优缺点总结)
15. [如何选型](#15-如何选型)
16. [学习建议](#16-学习建议)

---

## 1. 一句话理解 MongoDB 和 SQL 数据库

### SQL 数据库

SQL 数据库是以 **关系模型** 为核心的数据库。

它把数据拆成一张张结构严格的表，通过主键、外键、JOIN、事务和 SQL 查询来管理数据。

典型代表：

- MySQL
- PostgreSQL
- Oracle
- SQL Server
- MariaDB

适合：

- 数据结构稳定
- 关系复杂
- 强事务要求
- 报表统计
- 金融、订单、库存、账务等业务

### MongoDB

MongoDB 是以 **文档模型** 为核心的 NoSQL 数据库。

它把数据保存为类似 JSON 的 BSON 文档，天然支持嵌套对象、数组和灵活字段结构。

适合：

- 数据结构变化快
- 半结构化或非结构化数据
- 高并发读写
- 海量数据水平扩展
- 日志、IoT、内容系统、用户画像、车联网、智能驾驶元数据等场景

---

## 2. 核心差异总览

| 维度 | SQL 数据库 | MongoDB |
|---|---|---|
| 数据模型 | 表、行、列 | 文档、集合、字段 |
| 数据结构 | 强 Schema | 灵活 Schema |
| 查询语言 | SQL | MongoDB Query API / Aggregation Pipeline |
| 关系表达 | 外键、JOIN | 嵌入、引用、`$lookup` |
| 事务 | 天然强事务 | 支持事务，但设计上更鼓励文档级原子操作 |
| 扩展方式 | 传统上以垂直扩展为主，也可分库分表 | 原生分片，水平扩展能力强 |
| 数据一致性 | 通常强一致 | 可配置一致性，复制集支持 majority 读写 |
| 适合数据 | 结构化数据 | 半结构化、复杂嵌套、多变结构数据 |
| 开发模式 | 先设计表结构，再开发 | 常从应用访问模式出发设计文档 |
| 性能优化重点 | SQL 优化、JOIN 优化、索引、执行计划 | 文档模型、索引、分片键、聚合管道 |
| 典型场景 | 交易、财务、ERP、CRM、报表 | 内容、日志、IoT、用户画像、实时数据、地理位置、AI 元数据 |

---

## 3. 数据模型对比

## 3.1 SQL：表模型

SQL 数据库中，数据被拆分到不同表中。

例如，一个用户有多个地址，可以设计为：

```sql
CREATE TABLE users (
  id BIGINT PRIMARY KEY,
  name VARCHAR(100),
  email VARCHAR(100)
);

CREATE TABLE addresses (
  id BIGINT PRIMARY KEY,
  user_id BIGINT,
  type VARCHAR(20),
  city VARCHAR(100),
  street VARCHAR(200),
  FOREIGN KEY (user_id) REFERENCES users(id)
);
```

查询用户及地址时：

```sql
SELECT
  u.id,
  u.name,
  u.email,
  a.type,
  a.city,
  a.street
FROM users u
LEFT JOIN addresses a ON u.id = a.user_id
WHERE u.id = 1;
```

特点：

- 表结构清晰。
- 数据规范化程度高。
- 冗余少。
- 多表 JOIN 常见。
- 结构变更需要 DDL，如 `ALTER TABLE`。

---

## 3.2 MongoDB：文档模型

MongoDB 中，一个用户及其地址可以放在一个文档中：

```javascript
{
  _id: ObjectId("..."),
  name: "Alice",
  email: "alice@example.com",
  addresses: [
    {
      type: "home",
      city: "Beijing",
      street: "Chaoyang Road"
    },
    {
      type: "work",
      city: "Shanghai",
      street: "Pudong Road"
    }
  ]
}
```

查询用户：

```javascript
db.users.findOne({ _id: ObjectId("...") })
```

特点：

- 数据天然嵌套。
- 一次查询可以拿到完整对象。
- 字段结构灵活。
- 适合对象化、JSON 化的数据。
- 可能存在一定冗余。

---

## 3.3 数据模型核心区别

SQL 的核心思想是：

> 把数据拆开，减少冗余，通过关系重新组合。

MongoDB 的核心思想是：

> 把经常一起使用的数据放在一起，减少运行时关联成本。

这就是二者最根本的建模差异。

---

## 4. 使用场景对比

## 4.1 SQL 更适合的场景

### 1. 金融、账务、交易系统

例如：

- 银行转账
- 支付流水
- 账户余额
- 清结算
- 发票
- 财务凭证

这些场景通常要求：

- 强事务
- 强一致
- 数据结构稳定
- 审计严格
- 复杂约束

SQL 数据库在这些方面非常成熟。

---

### 2. ERP、CRM、供应链系统

这类系统有大量结构化实体：

- 客户
- 合同
- 订单
- 商品
- 库存
- 供应商
- 发货单
- 结算单

实体之间关系复杂，SQL 的外键、JOIN、事务和报表能力非常适合。

---

### 3. 强报表和复杂分析查询

SQL 对聚合、分组、窗口函数、复杂 JOIN、临时表、视图等支持成熟。

例如：

```sql
SELECT
  region,
  product_category,
  SUM(amount) AS total_sales,
  COUNT(*) AS order_count
FROM orders
WHERE order_date >= '2026-01-01'
GROUP BY region, product_category
ORDER BY total_sales DESC;
```

对于传统 BI、经营分析、财务报表，SQL 生态更完善。

---

## 4.2 MongoDB 更适合的场景

### 1. 数据结构变化快的业务

例如：

- 内容管理系统 CMS
- 活动配置
- 表单系统
- 用户画像
- 商品扩展属性
- 实验平台
- 规则引擎配置

MongoDB 不要求所有文档字段完全一致，适合快速迭代。

---

### 2. 半结构化和复杂嵌套数据

例如：

- JSON API 数据
- 设备上报数据
- 日志
- 传感器数据
- 用户行为事件
- 自动驾驶场景元数据

这些数据通常字段多、层级深、变化快，用关系表拆分会比较繁琐。

---

### 3. 高并发读写和海量数据

MongoDB 原生支持复制集和分片集群，适合水平扩展。

例如：

- 日志平台
- IoT 数据平台
- 车联网数据平台
- 实时事件系统
- 消息状态存储

---

### 4. 地理位置和时序数据

MongoDB 原生支持：

- GeoJSON
- `2dsphere` 地理空间索引
- 时间序列集合

适合：

- 附近门店
- 附近充电站
- 电子围栏
- 车辆轨迹
- 设备指标
- 传感器时序数据

---

### 5. AI 与向量搜索场景

MongoDB 可以同时存储业务元数据和向量 embedding。

适合：

- 知识库问答
- RAG 检索
- 自动驾驶场景相似片段召回
- 商品语义搜索
- 日志语义检索

---

## 4.3 简单选型判断

| 如果你的业务是…… | 更倾向选择 |
|---|---|
| 银行转账、余额、账务 | SQL |
| 订单、库存、合同、财务 | SQL |
| 复杂报表、BI 分析 | SQL / 专用 OLAP |
| 字段经常变化的内容系统 | MongoDB |
| JSON 数据原样存储和查询 | MongoDB |
| IoT、日志、车联网事件 | MongoDB |
| 地理位置检索 | MongoDB |
| 用户画像、动态标签 | MongoDB |
| 需要强事务且关系复杂 | SQL |
| 需要海量水平扩展且结构灵活 | MongoDB |

---

## 5. 架构设计对比

## 5.1 SQL 数据库典型架构

常见 SQL 架构：

```text
Application
    |
Database Driver / ORM
    |
SQL Database
    |
Tables / Indexes / Transactions
```

在规模扩大后，可能演进为：

```text
Application
    |
Load Balancer / Proxy
    |
Primary Database  ---- Replicas
    |
Sharding / Partitioning / Middleware
```

常见扩展手段：

- 主从复制
- 读写分离
- 分库分表
- 表分区
- 中间件路由
- 缓存层，如 Redis
- OLTP 与 OLAP 分离

---

## 5.2 MongoDB 典型架构

MongoDB 基础高可用架构是复制集：

```text
Application
    |
MongoDB Driver
    |
Replica Set
 ┌───────────┬────────────┬────────────┐
 │ Primary   │ Secondary  │ Secondary  │
 └───────────┴────────────┴────────────┘
```

分片集群架构：

```text
Application
    |
MongoDB Driver
    |
Mongos Router
    |
 ┌──────────────┬──────────────┬──────────────┐
 │ Shard 1      │ Shard 2      │ Shard 3      │
 │ Replica Set  │ Replica Set  │ Replica Set  │
 └──────────────┴──────────────┴──────────────┘
    |
Config Server Replica Set
```

组件说明：

| 组件 | 作用 |
|---|---|
| Application | 应用服务 |
| Driver | MongoDB 官方驱动，感知集群拓扑 |
| Mongos | 查询路由器，决定请求发到哪个分片 |
| Shard | 保存业务数据，通常每个 Shard 是复制集 |
| Config Server | 保存分片集群元数据 |
| Primary | 复制集主节点，负责写入 |
| Secondary | 从节点，复制数据，可读、可容灾 |

---

## 5.3 架构思想差异

SQL 传统架构更强调：

- 数据关系的规范性。
- 单库事务能力。
- 通过主从、分区、分库分表扩展。
- 数据一致性和约束。

MongoDB 架构更强调：

- 文档级数据聚合。
- 副本集高可用。
- 原生分片水平扩展。
- 数据模型与访问模式贴合。
- 通过减少 JOIN 和合理分片提升吞吐。

---

## 6. 底层存储逻辑对比

## 6.1 SQL 的底层逻辑

关系型数据库底层通常围绕以下机制构建：

1. **表空间 / 数据页**
2. **行存储**
3. **B+Tree 索引**
4. **事务日志 / redo log / WAL**
5. **undo / MVCC**
6. **锁机制**
7. **查询优化器**
8. **Buffer Pool / Shared Buffer**

以 MySQL InnoDB 为例：

- 数据按页存储。
- 主键索引通常是聚簇索引。
- 二级索引叶子节点保存主键值。
- 通过 redo log 保证崩溃恢复。
- 通过 undo log 和 MVCC 支持事务隔离。
- SQL 优化器选择执行计划。

PostgreSQL 也有类似思想，但实现不同：

- 使用堆表存储行。
- 索引指向 tuple 位置。
- 使用 WAL 做预写日志。
- 使用 MVCC 实现并发控制。
- 通过 VACUUM 清理旧版本数据。

---

## 6.2 MongoDB 的底层逻辑

MongoDB 现代版本默认使用 WiredTiger 存储引擎。

MongoDB 底层核心机制包括：

1. **BSON 文档存储**
2. **集合 Collection**
3. **WiredTiger 存储引擎**
4. **B-Tree 类索引结构**
5. **文档级并发控制**
6. **压缩机制**
7. **Journal 日志**
8. **Oplog 复制日志**
9. **复制集选主与同步**
10. **分片 Chunk 和 Balancer**

MongoDB 数据不是按二维表的行列逻辑组织，而是按 BSON 文档保存。

示例：

```javascript
{
  _id: ObjectId("..."),
  vin: "L1234567890",
  status: {
    speed: 80,
    battery: 76
  },
  tags: ["EV", "ADAS"]
}
```

这个文档作为一个整体存储和读取，内部字段可建立索引。

---

## 6.3 底层逻辑核心区别

| 维度 | SQL | MongoDB |
|---|---|---|
| 基本存储单位 | 行 Row | 文档 Document |
| 数据组织 | 表结构 | 集合中的 BSON 文档 |
| Schema | 预定义强结构 | 灵活结构，可选 Schema Validation |
| 索引 | B+Tree 等 | B-Tree 类索引、地理、文本、哈希、向量等 |
| 并发控制 | 行锁、MVCC 等 | 文档级并发控制、WiredTiger MVCC 机制 |
| 日志 | WAL / redo / undo | Journal、Oplog |
| 复制 | 主从、流复制、逻辑复制等 | 复制集、Oplog 同步 |
| 分布式扩展 | 常依赖分库分表或分布式数据库方案 | 原生 Sharding |

---

## 7. 查询语言和开发体验对比

## 7.1 SQL 查询

SQL 是声明式语言。

你告诉数据库“我要什么”，数据库优化器决定“怎么拿”。

示例：

```sql
SELECT id, name, email
FROM users
WHERE city = 'Beijing'
ORDER BY created_at DESC
LIMIT 10;
```

优点：

- 标准化程度高。
- 可读性强。
- 适合复杂查询。
- BI 和报表生态成熟。

---

## 7.2 MongoDB 查询

MongoDB 使用 JSON 风格查询 API。

```javascript
db.users.find(
  { city: "Beijing" },
  { name: 1, email: 1 }
).sort({ createdAt: -1 }).limit(10)
```

聚合使用 Aggregation Pipeline：

```javascript
db.orders.aggregate([
  { $match: { status: "paid" } },
  { $group: { _id: "$userId", total: { $sum: "$amount" } } },
  { $sort: { total: -1 } },
  { $limit: 10 }
])
```

优点：

- 与 JSON / JavaScript / 应用对象模型接近。
- 嵌套字段查询自然。
- 聚合管道表达能力强。
- 对开发者处理 API 数据友好。

---

## 7.3 查询语言差异

| 维度 | SQL | MongoDB |
|---|---|---|
| 查询风格 | 声明式 SQL | JSON 风格 API + Pipeline |
| 多表关联 | JOIN 原生强大 | `$lookup` 支持，但不宜滥用 |
| 嵌套数据 | 通常需拆表或 JSON 字段 | 天然支持 |
| 聚合 | `GROUP BY`、窗口函数 | Aggregation Pipeline、窗口函数 |
| 开发生态 | BI、报表、ORM 成熟 | Web/API/JSON 场景自然 |
| 学习曲线 | SQL 通用性强 | 文档模型和管道需要适应 |

---

## 8. 事务与一致性对比

## 8.1 SQL 的事务模型

SQL 数据库通常非常强调 ACID：

| ACID | 含义 |
|---|---|
| Atomicity 原子性 | 事务要么全部成功，要么全部失败 |
| Consistency 一致性 | 事务前后数据满足约束 |
| Isolation 隔离性 | 并发事务之间互不干扰或按隔离级别控制 |
| Durability 持久性 | 提交后数据不会丢失 |

示例：转账事务：

```sql
BEGIN;

UPDATE accounts SET balance = balance - 100 WHERE user_id = 1;
UPDATE accounts SET balance = balance + 100 WHERE user_id = 2;
INSERT INTO transfer_logs(from_user, to_user, amount) VALUES (1, 2, 100);

COMMIT;
```

SQL 数据库非常适合这种强事务、多表一致性场景。

---

## 8.2 MongoDB 的事务模型

MongoDB 单文档操作天然具备原子性。

例如：

```javascript
db.users.updateOne(
  { _id: userId },
  {
    $inc: { points: 10 },
    $push: { pointLogs: { reason: "order", points: 10, ts: new Date() } }
  }
)
```

这个更新对单个文档是原子的。

MongoDB 也支持多文档事务：

```javascript
const session = db.getMongo().startSession()

session.startTransaction()

try {
  const accounts = session.getDatabase("bank").accounts

  accounts.updateOne({ userId: 1 }, { $inc: { balance: -100 } })
  accounts.updateOne({ userId: 2 }, { $inc: { balance: 100 } })

  session.commitTransaction()
} catch (e) {
  session.abortTransaction()
  throw e
}
```

但 MongoDB 的设计哲学是：

> 尽量通过文档模型把需要一起原子更新的数据放在一个文档中，而不是频繁依赖跨文档事务。

---

## 8.3 事务对比

| 维度 | SQL | MongoDB |
|---|---|---|
| 单行/单文档原子性 | 支持 | 支持 |
| 多表/多文档事务 | 核心能力 | 支持，但应谨慎使用 |
| 设计倾向 | 通过事务维护多表一致性 | 通过文档聚合减少跨文档事务 |
| 强一致业务 | 非常适合 | 可实现，但需更谨慎设计 |
| 高并发写入 | 依赖锁/MVCC和索引设计 | 文档级并发，适合事件型写入 |

---

## 9. 扩展能力对比

## 9.1 SQL 的扩展

SQL 数据库常见扩展方式：

1. 垂直扩展：升级 CPU、内存、磁盘。
2. 读写分离：主库写，从库读。
3. 分区表：按时间、范围、列表分区。
4. 分库分表：应用或中间件负责路由。
5. 缓存：Redis、Memcached。
6. 分布式 SQL：TiDB、CockroachDB、YugabyteDB 等。

传统 SQL 扩展的难点：

- 分库分表后 JOIN 变复杂。
- 跨库事务复杂。
- 全局唯一 ID、全局排序、分页困难。
- 扩容迁移成本高。

---

## 9.2 MongoDB 的扩展

MongoDB 原生支持 Sharding。

基本思想：

- 选择一个分片键。
- 根据分片键把集合拆成多个 Chunk。
- Chunk 分布到不同 Shard。
- Mongos 根据查询条件路由请求。

示例：

```javascript
sh.enableSharding("vehicle")

sh.shardCollection(
  "vehicle.events",
  { vin: 1, ts: 1 }
)
```

优点：

- 原生水平扩展。
- 每个 Shard 可以是复制集。
- 应用通过 Mongos 访问，整体相对透明。
- 支持大规模数据和高吞吐。

难点：

- 分片键选择非常关键。
- 查询不带分片键可能广播到所有分片。
- 热点分片会影响整体性能。
- Chunk 迁移有成本。

---

## 9.3 扩展能力对比

| 维度 | SQL | MongoDB |
|---|---|---|
| 默认扩展方式 | 垂直扩展、主从 | 复制集、分片 |
| 水平扩展 | 传统方案较复杂 | 原生支持 |
| 分片路由 | 应用/中间件/分布式 SQL | Mongos |
| 跨分片 JOIN | 较复杂 | 不鼓励复杂跨分片关联 |
| 扩容难点 | 分库分表、跨库事务 | 分片键、热分片、路由效率 |

---

## 10. 索引与性能优化对比

## 10.1 SQL 索引优化

SQL 常见索引：

- 主键索引
- 唯一索引
- 普通索引
- 联合索引
- 全文索引
- 函数索引
- 部分索引，部分数据库支持
- GiST / GIN / BRIN，PostgreSQL 中常见

优化重点：

- WHERE 条件是否命中索引。
- JOIN 字段是否有索引。
- ORDER BY / GROUP BY 是否利用索引。
- 是否发生全表扫描。
- 是否有回表。
- 索引选择性是否足够。
- 执行计划是否合理。

查看执行计划：

```sql
EXPLAIN SELECT * FROM orders WHERE user_id = 100;
```

---

## 10.2 MongoDB 索引优化

MongoDB 常见索引：

- 单字段索引
- 联合索引
- 多键索引
- 哈希索引
- 地理空间索引
- 文本索引
- TTL 索引
- 部分索引
- 稀疏索引
- 隐藏索引
- 向量索引，通常在 Atlas Vector Search 场景

查看执行计划：

```javascript
db.orders.find({ userId: 100 }).explain("executionStats")
```

关注字段：

- `COLLSCAN`：集合扫描，通常要避免。
- `IXSCAN`：索引扫描。
- `FETCH`：根据索引结果取文档。
- `totalKeysExamined`：扫描索引键数量。
- `totalDocsExamined`：扫描文档数量。
- `nReturned`：返回数量。

---

## 10.3 索引设计思想差异

SQL 索引设计常围绕：

- JOIN 字段。
- WHERE 条件。
- ORDER BY / GROUP BY。
- 覆盖索引。
- 事务写入成本。

MongoDB 索引设计常围绕：

- 文档访问模式。
- 嵌套字段查询。
- 数组字段查询。
- 排序和范围过滤。
- ESR Rule。
- 分片键和查询路由。
- 写入成本和索引数量控制。

---

## 11. 表关联、嵌套和聚合对比

## 11.1 SQL：通过 JOIN 组合数据

例如订单、用户、商品：

```sql
SELECT
  o.id,
  u.name,
  p.name AS product_name,
  oi.quantity
FROM orders o
JOIN users u ON o.user_id = u.id
JOIN order_items oi ON o.id = oi.order_id
JOIN products p ON oi.product_id = p.id
WHERE o.id = 1001;
```

优点：

- 数据规范。
- 冗余少。
- 关联表达清晰。

缺点：

- 多表 JOIN 成本高。
- 分库分表后 JOIN 复杂。
- 对高并发接口可能不够直接。

---

## 11.2 MongoDB：通过嵌入减少关联

订单可以设计为一个文档：

```javascript
{
  _id: ObjectId("..."),
  orderNo: "O1001",
  user: {
    userId: 1,
    name: "Alice"
  },
  items: [
    {
      productId: 101,
      productName: "Keyboard",
      price: 299,
      quantity: 1
    },
    {
      productId: 102,
      productName: "Mouse",
      price: 99,
      quantity: 2
    }
  ],
  status: "paid",
  createdAt: ISODate("2026-06-08T08:00:00Z")
}
```

查询订单详情：

```javascript
db.orders.findOne({ orderNo: "O1001" })
```

优点：

- 一次查询拿到订单详情。
- 接近 API 返回结构。
- 减少 JOIN。

缺点：

- 用户名、商品名可能冗余。
- 商品信息变化后历史订单是否同步需要业务决策。
- 文档不能无限增长。

---

## 11.3 MongoDB 的 `$lookup`

MongoDB 也支持类似 JOIN 的 `$lookup`：

```javascript
db.orders.aggregate([
  {
    $lookup: {
      from: "users",
      localField: "userId",
      foreignField: "_id",
      as: "user"
    }
  }
])
```

但 MongoDB 中不建议把 `$lookup` 当作默认建模手段。更推荐优先通过访问模式判断是否嵌入、引用或冗余。

---

## 12. Schema 设计思路对比

## 12.1 SQL Schema 设计

SQL 设计通常从实体关系出发：

1. 识别实体。
2. 识别属性。
3. 识别关系。
4. 做范式化设计。
5. 定义主键、外键、唯一约束。
6. 定义索引。
7. 根据性能做反范式优化。

例如：

- 用户表
- 订单表
- 商品表
- 订单明细表
- 支付流水表

关系非常清晰。

---

## 12.2 MongoDB Schema 设计

MongoDB 设计通常从访问模式出发：

1. 这个接口或业务流程需要读什么数据？
2. 哪些字段总是一起读取？
3. 哪些字段需要一起原子更新？
4. 子数据会不会无限增长？
5. 是否需要独立查询？
6. 是否需要索引？
7. 是否需要分片？

MongoDB 常见设计策略：

- 嵌入。
- 引用。
- 冗余。
- 预聚合。
- 子集模式。
- 桶模式。
- 多态模式。
- 属性模式。
- 事件模式。

---

## 12.3 范式化 vs 反范式化

SQL 通常倾向范式化：

```text
users
orders
order_items
products
```

MongoDB 通常更接受反范式化：

```text
orders 文档中嵌入 user 快照和 item 快照
```

区别不是谁更高级，而是优化目标不同：

- SQL 优化数据一致性和关系表达。
- MongoDB 优化读取路径和对象聚合。

---

## 13. 典型业务建模对比

## 13.1 用户画像

### SQL 设计

```sql
CREATE TABLE users (
  id BIGINT PRIMARY KEY,
  name VARCHAR(100)
);

CREATE TABLE user_tags (
  user_id BIGINT,
  tag VARCHAR(100)
);

CREATE TABLE user_properties (
  user_id BIGINT,
  property_key VARCHAR(100),
  property_value VARCHAR(500)
);
```

优点：结构规范。  
缺点：动态属性多时查询复杂。

### MongoDB 设计

```javascript
{
  _id: ObjectId("..."),
  userId: 1001,
  name: "Alice",
  tags: ["high_value", "ev_owner", "beijing"],
  properties: {
    ageRange: "30-35",
    preferredBrand: "Tesla",
    lastActiveChannel: "app"
  },
  scores: {
    churnRisk: 0.13,
    purchaseIntent: 0.82
  }
}
```

优点：灵活，适合动态标签和画像字段。

---

## 13.2 订单系统

### SQL 更典型

订单系统通常适合 SQL，因为它涉及：

- 库存扣减
- 支付
- 优惠券
- 余额
- 发票
- 售后
- 财务对账

这些都需要较强事务和关系约束。

### MongoDB 也可用于部分订单读模型

例如订单详情页读模型：

```javascript
{
  orderNo: "O1001",
  userSnapshot: {
    userId: 1,
    name: "Alice"
  },
  itemSnapshots: [
    { sku: "K001", name: "Keyboard", price: 299, qty: 1 }
  ],
  status: "paid",
  timeline: [
    { status: "created", ts: ISODate("2026-06-08T08:00:00Z") },
    { status: "paid", ts: ISODate("2026-06-08T08:01:00Z") }
  ]
}
```

实践中，也可以：

- SQL 做交易事实源。
- MongoDB 做订单详情读模型、用户行为、物流轨迹、客服上下文。

---

## 13.3 车联网事件

### SQL 设计问题

车辆事件字段变化多：

- 速度
- 电量
- 温度
- 故障码
- ADAS 状态
- GPS
- 传感器版本
- 软件版本

如果用 SQL，可能需要：

- 很宽的表。
- JSON 字段。
- EAV 模型。
- 多张事件子表。

复杂度会上升。

### MongoDB 设计

```javascript
{
  vin: "L1234567890",
  eventType: "battery_warning",
  ts: ISODate("2026-06-08T08:00:00Z"),
  location: {
    type: "Point",
    coordinates: [116.4825, 39.9414]
  },
  payload: {
    soc: 18,
    temperature: 45.2,
    warningCode: "BAT_TEMP_HIGH"
  },
  softwareVersion: "2026.06.01"
}
```

更适合字段多变、半结构化、高写入场景。

---

## 14. 优缺点总结

## 14.1 SQL 数据库优点

- 理论基础成熟。
- SQL 标准通用。
- 强事务能力优秀。
- 复杂 JOIN 和报表能力强。
- 数据一致性和约束能力强。
- 生态成熟，工具丰富。
- 适合结构化核心业务数据。

## 14.2 SQL 数据库缺点

- Schema 变更成本较高。
- 半结构化数据处理不够自然。
- 高并发海量写入需要复杂架构。
- 分库分表后复杂度明显上升。
- 对深层嵌套 JSON 类业务不够直观。

---

## 14.3 MongoDB 优点

- 文档模型灵活。
- 与 JSON/API/对象模型贴近。
- 嵌套数据表达自然。
- 适合快速迭代。
- 原生复制集和分片。
- 支持地理空间、时序、文本、向量等多种数据形态。
- 高并发事件型数据处理能力强。

## 14.4 MongoDB 缺点

- 建模自由度高，也更容易设计不当。
- 不适合滥用跨文档事务和复杂关联。
- 分片键选择难度较高。
- 数据冗余需要业务治理。
- 对传统复杂 SQL 报表生态不如关系型数据库自然。
- 对强约束、强规范业务，需要额外设计。

---

## 15. 如何选型

## 15.1 优先选择 SQL 的情况

如果你的系统满足以下特征，优先考虑 SQL：

- 数据结构稳定。
- 实体关系复杂。
- 强事务、多表一致性要求高。
- 需要外键、唯一约束、复杂约束。
- 需要大量 JOIN 和复杂报表。
- 是账务、库存、订单、合同、ERP、CRM 等核心系统。

典型例子：

- 银行账户系统
- 电商交易主链路
- 财务核算系统
- 库存扣减系统
- 人事薪资系统

---

## 15.2 优先选择 MongoDB 的情况

如果你的系统满足以下特征，优先考虑 MongoDB：

- 数据结构经常变化。
- 数据天然是 JSON 或对象结构。
- 存在大量嵌套字段和数组。
- 读接口希望一次返回完整对象。
- 高并发写入事件、日志、设备数据。
- 需要水平扩展。
- 需要地理位置、时序、文本、向量等能力。
- 业务强调快速迭代。

典型例子：

- 内容管理系统
- 用户画像系统
- IoT 数据平台
- 车联网事件平台
- 自动驾驶场景元数据管理
- 日志检索平台
- 知识库和 RAG 元数据存储

---

## 15.3 混合架构往往更现实

真实业务中，不一定只能二选一。

常见组合：

```text
SQL：订单、支付、库存、财务等强事务核心数据
MongoDB：用户行为、日志、画像、配置、内容、车联网事件、AI 元数据
Redis：缓存、排行榜、分布式锁
Elasticsearch / Search：复杂全文检索
OLAP：数据仓库、报表分析
```

例如电商系统：

| 模块 | 推荐存储 |
|---|---|
| 用户账号 | SQL |
| 订单交易 | SQL |
| 库存扣减 | SQL |
| 商品扩展属性 | MongoDB |
| 用户行为日志 | MongoDB / 日志系统 |
| 搜索 | Elasticsearch / MongoDB Search |
| 推荐特征 | MongoDB / Feature Store |
| 财务报表 | SQL / OLAP |

例如智能汽车系统：

| 模块 | 推荐存储 |
|---|---|
| 车主账号、合同、订单 | SQL |
| 车辆档案和动态配置 | MongoDB |
| 车端事件和日志 | MongoDB |
| 传感器时序指标 | MongoDB Time Series / TSDB |
| 地理围栏和充电站 | MongoDB Geo |
| 自动驾驶片段元数据 | MongoDB |
| 训练样本和标签 | MongoDB + 对象存储 |
| 财务、结算、发票 | SQL |

---

## 16. 学习建议

## 16.1 如果你已有 SQL 基础

建议这样学习 MongoDB：

1. 不要先把表直接翻译成集合。
2. 先理解文档模型和嵌入设计。
3. 多思考接口访问模式。
4. 学会用 `explain()` 看索引是否命中。
5. 理解复制集和分片，而不是只学 CRUD。
6. 学习地理空间、时间序列、聚合管道等特色能力。

---

## 16.2 如果你已有 MongoDB 基础

建议补 SQL：

1. 学习关系模型和范式。
2. 学习 JOIN、GROUP BY、窗口函数。
3. 学习事务隔离级别。
4. 学习执行计划和索引设计。
5. 理解为什么强交易系统更偏向 SQL。

---

## 16.3 对比学习路线

| 阶段 | SQL 学习重点 | MongoDB 学习重点 |
|---|---|---|
| 入门 | 表、行、列、SQL CRUD | 文档、集合、MongoDB CRUD |
| 建模 | 主键、外键、范式 | 嵌入、引用、访问模式 |
| 查询 | JOIN、GROUP BY、窗口函数 | find、aggregate、pipeline |
| 性能 | B+Tree、执行计划、慢 SQL | 索引、explain、ESR Rule |
| 事务 | ACID、隔离级别、锁 | 单文档原子性、多文档事务 |
| 高可用 | 主从、复制、故障切换 | 复制集、选主、读写关注 |
| 扩展 | 分区、分库分表、分布式 SQL | Sharding、分片键、Chunk |
| 专项 | BI、报表、OLAP | 地理、时序、搜索、向量 |

---

## 最后总结

MongoDB 和 SQL 数据库不是简单的“谁替代谁”的关系，而是面向不同数据形态和业务问题的两类重要数据库技术。

SQL 数据库强在：

> 结构化、强事务、强关系、强约束、复杂查询和成熟生态。

MongoDB 强在：

> 灵活文档模型、复杂嵌套数据、快速迭代、高并发事件数据、水平扩展和多模态数据能力。

选型时最重要的问题不是“哪个数据库更先进”，而是：

1. 我的数据结构是否稳定？
2. 我的数据关系是否复杂？
3. 我的核心诉求是强事务，还是灵活扩展？
4. 我的查询模式是多表关联，还是按对象整体读取？
5. 我的数据规模和写入压力是否需要水平扩展？
6. 我的业务是否包含 JSON、地理位置、时序、日志、向量等数据？

如果答案偏向强关系、强事务、强约束，SQL 更合适。  
如果答案偏向灵活结构、复杂文档、高并发事件和水平扩展，MongoDB 更合适。  
在大型系统中，二者组合使用往往是最合理的架构。
